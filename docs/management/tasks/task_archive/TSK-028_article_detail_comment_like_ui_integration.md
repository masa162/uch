# タスク詳細: TSK-028

**ID**: `TSK-028`
**タイトル**: 記事詳細・コメント・いいねのUI連携
**ステータス**: 未着手
**優先度**: 高

## 1. タスクの目的

記事詳細ページ、コメント機能、いいね機能を、それぞれのAPIと連携させ、ユーザーが記事を閲覧し、コメントやいいねでインタラクションできるようにする。

## 2. 手順

### 2.1. 記事詳細ページ (`src/app/articles/[slug]/page.tsx`) の新規作成

*   **目的**: 記事詳細取得API (`/api/articles/[slug]`) と連携し、記事の内容を表示する。
*   **手順**: `/Users/nakayamamasayuki/Documents/GitHub/uch/src/app/articles/[slug]/page.tsx` を新規作成し、記事詳細を表示するコンポーネントを実装する。

### 2.2. コメント機能のUIとAPIの連携

*   **目的**: コメント投稿・取得API (`/api/articles/[slug]/comments`) と連携し、コメントの表示と投稿ができるようにする。
*   **手順**: コメント表示・投稿用のUIコンポーネントを実装し、対応するAPIを呼び出すロジックを追加する。

### 2.3. いいね機能のUIとAPIの連携

*   **目的**: いいね登録・削除API (`/api/articles/[slug]/like`) と連携し、いいねの表示と操作ができるようにする。
*   **手順**: いいね表示・操作用のUIコンポーネントを実装し、対応するAPIを呼び出すロジックを追加する。

## 3. 完了の定義

*   記事詳細ページが実装され、記事内容が表示されること。
*   コメント機能が実装され、コメントの表示と投稿ができること。
*   いいね機能が実装され、いいねの表示と操作ができること。

## 4. 検証方法

PMがコードを読み取り、API連携ロジックが意図通りに実装されていることを確認する。
