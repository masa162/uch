# タスク詳細: TSK-026

**ID**: `TSK-026`
**タイトル**: トップページと記事一覧のデータ連携
**ステータス**: 未着手
**優先度**: 高

## 1. タスクの目的

トップページや記事一覧ページを、記事投稿・管理APIと連携させ、実際の記事データを表示・操作できるようにする。これにより、アプリケーションの主要コンテンツが表示されるようになる。

## 2. 手順

### 2.1. `src/app/page.tsx` の修正

*   **目的**: 記事一覧取得API (`/api/articles`) を呼び出し、トップページに記事のリストを表示する。
*   **修正箇所**: `page.tsx` 内で記事データをフェッチし、表示するロジックを追加する。

### 2.2. 記事一覧ページ (`src/app/articles/page.tsx`) の新規作成

*   **目的**: 記事一覧ページを実装し、記事一覧取得API (`/api/articles`) と連携させる。
*   **手順**: `/Users/nakayamamasayuki/Documents/GitHub/uch/src/app/articles/page.tsx` を新規作成し、記事一覧を表示するコンポーネントを実装する。

## 3. 完了の定義

*   トップページに記事一覧が表示されること。
*   記事一覧ページが実装され、記事一覧が表示されること。

## 4. 検証方法

PMがコードを読み取り、API連携ロジックが意図通りに実装されていることを確認する。
